/*1) Проверка значения из диапазона

Напишите условие if для проверки, что переменная age находится в диапазоне между 14 и 90 включительно.

«Включительно» означает, что значение переменной age может быть равно 14 или 90.

-------------------------------------------------------------------------------------------- */
// let age = 14; 

// if (age >= 14 && age <= 90) {
//     console.log("Возраст находится в диапазоне.");
// } else {
//     console.log("Возраст не находится в диапазоне.");
// }








/*2) Проверка значения вне диапазона

Напишите условие if для проверки, что значение переменной age НЕ находится в диапазоне 14 и 90 включительно.

Напишите два варианта: первый с использованием оператора НЕ !, второй – без этого оператора.

--------------------------------------------------------------------------------------------*/

// let age = 30; // Здесь можно изменить значение age для тестирования

// // Вариант 1: с использованием оператора НЕ (!)
// if (!(age >= 14 && age <= 90)) {
//     console.log("Возраст НЕ находится в диапазоне.");
// } else {
//     console.log("Возраст находится в диапазоне.");
// }





/*3) Проверка логина

Напишите код, который будет спрашивать логин с помощью prompt.

Если посетитель вводит «Админ», то prompt запрашивает пароль, если ничего не введено или нажата клавиша Esc – показать «Отменено», в противном случае отобразить «Я вас не знаю».

Пароль проверять так:

Если введён пароль «Я главный», то выводить «Здравствуйте!»,
Иначе – «Неверный пароль»,
При отмене – «Отменено».
Блок-схема:
Для решения используйте вложенные блоки if. Обращайте внимание на стиль и читаемость кода.

Подсказка: передача пустого ввода в приглашение prompt возвращает пустую строку ''. Нажатие клавиши Esc во время запроса возвращает null.

*/



// let login = prompt("Введите логин:");

// if (login === "Админ") {
//     let password = prompt("Введите пароль:");

//     if (password === "Я главный") {
//         alert ("Здравствуйте!");

//     } else if (password === null) {
//         alert("Отменено");

//     } else {
//         alert("Неверный пароль");
//     }
// } else if (login === null) {
//     alert("Отменено");
// } else {
//     alert("Я вас не знаю");
// }



// let a = 1, b = 1;

// let c = ++a;
// let d = b++;
// console.log (a+b+c+d)

// let a = 2; 

// let x = 1 + (a*=2);

// "" + 1 + 0 = '10'
// "" - 1 + 0 = -1
// true + false = true
// 6 / "3" = 2
// "2" * "3"= '6'
// 4 + 5 + "px" = 9px
// "$" + 4 + 5 = $45
// "4" - 2 = 4
// "4px" - 2 = 4px
// "  -9  " + 5 = ' -9  5'
// "  -9  " - 5 = -14
// null + 1 = 1 
// undefined + 1 = NaN
// " \t \n" - 2 = - 2

// let a = prompt("Первое число?" 1);
// let b = prompt("Второе число?", 2);

// alert(a+b); 

// 5 > 4 = true;
// "ананас" > "яблоко" = false;
// "2" > "12" = true;
// undefined == null; true
// undefined === null; false
// null == "\n0\n"= false
// null === +"\n0\n" = false


// alert( null || 2 || undefined); = 2
// alert( alert(1) || 2 || alert(3) ); = 1
// alert( 1 && null && 2); = null
// alert( alert(1) && alert(2) ); = 1
// alert( null || 2 && 3 || 4); = 3


// if (-1 || 0) alert( 'first'); = first 
// if (-1 && 0) alert( 'second' ); = no answer
// if (null || -1 && 1) alert( 'third' ) = third







